export const selectAllReservations = (state) =>
  state.reservation.allReservations;
export const selectMyReservations = (state) => state.reservation.myReservations;
export const selectReservationDetails = (state) =>
  state.reservation.reservationDetails;
